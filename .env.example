# Variables de entorno para desarrollo
NODE_ENV=development
MONGO_URI_DEV=mongodb://localhost:27017/obligatoriofs

# Variables de entorno para producción (Vercel)
# NODE_ENV=production
# MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/obligatoriofs

# JWT Secret (debe ser una cadena aleatoria segura)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Puerto (opcional, Vercel lo maneja automáticamente)
PORT=3000